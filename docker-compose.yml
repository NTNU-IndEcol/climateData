version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./download:/app/download
      - ./app:/app/app
    environment:
      - FLASK_ENV=production
    working_dir: /app
    restart: unless-stopped